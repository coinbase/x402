# x402 Error Handling Example Configuration

# Resource server to test against (REQUIRED)
RESOURCE_SERVER_URL=https://api.x402.org

# Payment credentials (at least one required)
EVM_PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
SVM_PRIVATE_KEY=base58_private_key_here

# API endpoint to test (optional, defaults to /api/data)
ENDPOINT_PATH=/api/data

# Custom facilitator URL (optional, uses default if not set)
FACILITATOR_URL=https://facilitator.x402.org

# Request timeout in seconds (optional, default: 30.0)
REQUEST_TIMEOUT=30.0

# Maximum retry attempts (optional, default: 3)
MAX_RETRIES=3