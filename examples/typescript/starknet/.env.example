# Starknet x402 Facilitator Server Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3000
NODE_ENV=production

# Network Configuration
# Options: "starknet" (mainnet) or "starknet-sepolia" (testnet)
STARKNET_NETWORK=starknet-sepolia

# Optional: Custom RPC URL (if not provided, uses default)
# STARKNET_RPC_URL=https://your-starknet-rpc-endpoint.com

# Facilitator Account Configuration
# The private key of the facilitator account (keep this secret!)
FACILITATOR_PRIVATE_KEY=0xYourPrivateKeyHere

# The address of the facilitator account
FACILITATOR_ADDRESS=0xYourFacilitatorAddressHere

# Rate Limiting Configuration
# Maximum amount allowed per day (in atomic units, e.g., 1000000 = 1 USDC with 6 decimals)
MAX_AMOUNT_PER_DAY=1000000000

# Maximum number of transactions allowed per day per account
MAX_TRANSACTIONS_PER_DAY=100

# Enable/disable rate limiting (true/false)
ENABLE_RATE_LIMITING=true

# Enable/disable session keys support (true/false)
ENABLE_SESSION_KEYS=true

# CORS Configuration
# Set to specific origin in production (e.g., https://yourdomain.com)
CORS_ORIGIN=*

# Optional: Database Configuration (for persistent state)
# DATABASE_URL=postgresql://user:password@localhost:5432/x402_facilitator

# Optional: Redis Configuration (for distributed rate limiting)
# REDIS_URL=redis://localhost:6379

# Optional: Monitoring Configuration
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# LOG_LEVEL=info

# Optional: Webhook Configuration (for payment notifications)
# WEBHOOK_URL=https://your-webhook-endpoint.com/payments
# WEBHOOK_SECRET=your-webhook-secret