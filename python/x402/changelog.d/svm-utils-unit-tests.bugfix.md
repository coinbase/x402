Add unit tests for SVM utility functions (`get_network_config`, `get_asset_info`, `parse_amount`, `format_amount`, `parse_money_to_decimal`) in `tests/unit/mechanisms/svm/test_utils.py`. These pure functions had no direct test coverage; 24 new tests cover all three Solana networks, V1 legacy name normalisation, asset address validation, amount conversion round-trips, and currency-string parsing.
