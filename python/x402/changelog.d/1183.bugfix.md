Fixed get_asset_info() returning incorrect metadata for unregistered tokens. Functions in both EVM and SVM mechanisms now raise ValueError for unknown asset addresses instead of returning misleading default token metadata with the custom address.
