Added configurable gas limit support to ``FacilitatorWeb3Signer`` — constructor now accepts optional ``gas_limit`` parameter (default: 300,000), and both ``write_contract()`` and ``send_transaction()`` methods accept per-call ``gas`` parameter overrides for flexible gas management across different contract call complexities and network conditions.