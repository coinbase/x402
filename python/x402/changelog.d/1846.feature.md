Make gas limit configurable in FacilitatorWeb3Signer. Added constructor parameter gas_limit (default: 300,000) and optional gas parameter to write_contract() and send_transaction() methods for flexibility with different contract call complexities and network conditions.
