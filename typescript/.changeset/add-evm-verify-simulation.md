---
"@x402/evm": minor
"@x402/extensions": patch
---

Add transaction simulation to the EVM facilitator verify path and require `simulateContract` on `FacilitatorEvmSigner` to catch reverting settlements before gas is spent.

