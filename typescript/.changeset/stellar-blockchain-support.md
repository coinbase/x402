---
"@x402/stellar": major
---

Add Stellar blockchain support for the x402 v2 payment protocol. Implements the exact scheme with CAIP-2 network identifiers and SEP-41 token contracts (defaults to USDC on Stellar).
